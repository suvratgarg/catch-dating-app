# package:test configuration
# https://github.com/dart-lang/test/blob/master/pkgs/test/doc/configuration.md

tags:
  # Exact PNG visual-regression tests. These are intentionally excluded from
  # Ubuntu Flutter CI because host font rasterization changes the pixels.
  golden: {}
