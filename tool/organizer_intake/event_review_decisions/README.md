# Event Review Decisions

Repo-backed manual decisions for external event candidates.

Each JSON file is generated by `event_review_decision.mjs` or exported from
Firestore by `export_event_review_decisions_from_firestore.mjs`. These decisions
only annotate generated event candidates for future import planning. They do not
write `events`, notify hosts, or enable recurring provider crawls.
