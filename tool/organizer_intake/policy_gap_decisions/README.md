# Organizer Policy Decisions

Live decisions exist only in
`organizerPolicyGapReviewDecisions/{decisionId}` through
`adminDecideOrganizerPolicyGap`.

They record review state and do not enable a crawler, provider integration,
event import, default, or naming migration. Repository policy-decision JSON is
retired and ignored.
