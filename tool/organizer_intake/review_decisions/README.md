# Organizer Review Decisions

Live decisions exist only in
`organizerIntakeReviewDecisions/{entityId}` through
`adminDecideOrganizerIntake`.

Repository decision JSON is retired and ignored. Use the authenticated Admin
workbench; do not export Firestore back into this directory.
