{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://catch.app/contracts/callable_responses/submit_event_runtime_profile_response.schema.json",
  "title": "SubmitEventRuntimeProfileCallableResponse",
  "description": "Server-recomputed Event Success runtime profile readiness.",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "requiredFieldIds", "completedFieldIds"],
  "properties": {
    "status": {"type": "string", "enum": ["needsInput", "ready"]},
    "requiredFieldIds": {"type": "array", "items": {"type": "string"}, "maxItems": 5},
    "completedFieldIds": {"type": "array", "items": {"type": "string"}, "maxItems": 5}
  }
}
