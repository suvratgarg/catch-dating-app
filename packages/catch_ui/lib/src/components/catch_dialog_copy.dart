/// Caller-resolved defaults for a typed confirmation dialog.
class CatchDialogCopy {
  const CatchDialogCopy({
    required this.cancelLabel,
    required this.confirmLabel,
  });

  final String cancelLabel;
  final String confirmLabel;
}
