/// Resolved title and action labels for a date or time picker sheet.
class CatchPickerCopy {
  const CatchPickerCopy({
    required this.title,
    required this.cancelLabel,
    required this.doneLabel,
  });

  final String title;
  final String cancelLabel;
  final String doneLabel;
}
