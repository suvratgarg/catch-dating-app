name: catch_ui_lints
description: Catch-specific analyzer plugin for semantic Flutter UI rules.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  analysis_server_plugin: ^0.3.20
  analyzer: ^14.1.0

dev_dependencies:
  flutter:
    sdk: flutter
  lints: ^6.0.0
