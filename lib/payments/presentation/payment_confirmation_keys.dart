import 'package:flutter/widgets.dart';

abstract final class PaymentConfirmationKeys {
  static const addToCalendar = ValueKey('payment-confirmation-add-calendar');
  static const directions = ValueKey('payment-confirmation-directions');
  static const inviteFriend = ValueKey('payment-confirmation-invite-friend');
  static const referralShare = ValueKey('payment-confirmation-referral-share');
  static const backHome = ValueKey('payment-confirmation-back-home');
}
