{
  "schemaVersion": 1,
  "rules": [
    {
      "id": "route-and-screen-surfaces",
      "owner": "ui_capture_pipeline",
      "selector": {
        "annotatedTypePattern": "(?:Screen|MyApp|ForceUpdateGate)$"
      },
      "disposition": "migrate-to-ui-capture",
      "reason": "Full route and screen lifecycle states belong in the UI capture pipeline, where navigation, viewport and provider behavior are asserted together.",
      "replacementGate": "Retire a Widgetbook case only after an equivalent checked capture target is present."
    },
    {
      "id": "provider-scoped-component-fixtures",
      "owner": "ui_system_program",
      "selector": {
        "annotatedTypeExcludesPattern": "(?:Screen|MyApp|ForceUpdateGate)$"
      },
      "disposition": "keep-widgetbook",
      "reason": "Keep bounded production component and body matrices whose injected provider failures, partial data or controller states are not stable route-capture targets."
    }
  ]
}
